# 第 1 节 · 认识 Web，写出你的第一个网页

> 目标：花 1 小时左右，搞懂"网页是什么"，并亲手写出一个能在浏览器打开的页面。

## 学习目标

学完这一节，你应该能做到：

1. 用自己的话说出"网页"是什么、浏览器到底在干什么
2. 说出 HTML、CSS、JavaScript 各自的角色
3. 装好开发工具，写出第一个 `.html` 文件并在浏览器里打开
4. 看懂并使用 5 个最常用的 HTML 标签

---

## 一、网页到底是什么？

打开淘宝、知乎、B 站时，你看到的花花绿绿的页面，**本质上就是一份特殊格式的文本文件**。

你可以做个小实验：

- 用 Chrome 打开任意网页
- 在页面上点右键 → "查看网页源代码"

你会看到一大堆带尖括号的文字，比如 `<div>`、`<p>`、`<a href="...">`。
这些文字就是"网页的源代码"，浏览器读到这些代码后，把它们"翻译"成了你眼前漂亮的页面。

**一句话总结：网页 = 一份用特定语法写的文本，浏览器负责把它显示成你看到的样子。**

## 二、浏览器在做什么？

可以把浏览器想象成一个"翻译官 + 排版工"：

```
你写的代码 (.html 文件)
        ↓
   浏览器读取
        ↓
   解析、排版、上色
        ↓
你看到的网页（带文字、图片、按钮）
```

常见的浏览器：Chrome、Edge、Safari、Firefox。
**学习阶段统一用 Chrome**，方便后面调试。

## 三、Web 三大基石

一个网页通常由三种语言协作完成，分工很清楚：

| 语言 | 作用 | 类比 |
| --- | --- | --- |
| HTML | 决定页面**有什么内容、什么结构** | 房子的**骨架** |
| CSS | 决定**长什么样**（颜色、字体、布局） | 房子的**装修** |
| JavaScript | 决定**能做什么**（点击、动画、交互） | 房子里的**电器** |

> 本节我们只学 HTML。CSS 和 JavaScript 留到后面几节。

## 四、准备工具

只需要两个：

### 1. 浏览器：Chrome

如果你电脑上还没有，去 [https://www.google.cn/chrome/](https://www.google.cn/chrome/) 下载安装。

### 2. 代码编辑器：VS Code

普通的记事本也能写代码，但 VS Code 会自动高亮、补全标签，对新手非常友好。

下载地址：[https://code.visualstudio.com/](https://code.visualstudio.com/)

安装好后，先熟悉这几个动作：

- `File → New File` 新建文件
- `File → Save` 保存（保存时记得文件名带 `.html` 后缀）
- 拖文件夹到 VS Code 窗口可以打开整个项目

## 五、动手 1：写出你的第一个网页

### 第 1 步：新建文件

在电脑上随便找个地方（比如桌面）新建一个文件夹叫 `web-101`，
用 VS Code 打开这个文件夹，在里面新建一个文件，**命名为 `hello.html`**。

### 第 2 步：写下这段代码

把下面的代码**完整地**复制进去：

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>My First Page</title>
  </head>
  <body>
    <h1>Hello, Web!</h1>
    <p>This is my first web page.</p>
  </body>
</html>
```

### 第 3 步：在浏览器里打开

保存文件后，在文件管理器里双击 `hello.html`，
或者右键 → "打开方式" → 选择 Chrome。

如果一切正常，你会看到一个白底页面，最上面是大字 "Hello, Web!"，下面跟着一行小字。

### 这段代码到底在说什么？

不用全部记住，先有个印象：

- `<!DOCTYPE html>` — 告诉浏览器："这是 HTML5 写的文件"
- `<html>` ... `</html>` — 整个网页的最外层"信封"
- `<head>` — 网页的"配置信息"，**用户看不到**（比如标题、字符编码）
- `<body>` — **用户能看到**的内容都写在这里
- `<h1>` — 一级大标题
- `<p>` — 一段普通段落

> 💡 注意：HTML 标签都是**成对**出现的，比如 `<h1>...</h1>`，
> 前面那个叫"开标签"，后面那个叫"闭标签"，中间夹的就是内容。

## 六、动手 2：再加点内容

把你 `hello.html` 里 `<body>` 的内容替换成下面这样，保存后**刷新浏览器**看效果：

```html
<body>
  <h1>About Me</h1>

  <p>Hi, my name is Tom. I am learning web development.</p>

  <h2>My Hobbies</h2>
  <ul>
    <li>Reading</li>
    <li>Running</li>
    <li>Coding</li>
  </ul>

  <p>
    Visit my favorite site:
    <a href="https://www.wikipedia.org">Wikipedia</a>
  </p>

  <img
    src="https://placekitten.com/300/200"
    alt="A cute kitten"
  />
</body>
```

刷新浏览器后，你应该能看到：标题、段落、列表、一个可点击的链接、一张猫咪图片。

### 这一节登场的 5 个标签

| 标签 | 作用 |
| --- | --- |
| `<h1>` ~ `<h6>` | 标题，从大到小，6 级 |
| `<p>` | 段落 |
| `<ul>` / `<li>` | 无序列表（带圆点的列表） |
| `<a href="...">` | 链接，点击会跳转到 `href` 里的网址 |
| `<img src="..." alt="..." />` | 图片，`src` 是图片地址，`alt` 是图片加载失败时显示的文字 |

> `<a>` 和 `<img>` 里的 `href`、`src`、`alt` 这种 "name=value" 的写法叫**属性**（attribute），
> 它们用来给标签提供额外信息。

## 七、本节小结

- 网页 = 文本文件 + 浏览器渲染
- HTML 负责**内容和结构**，CSS 管样式，JS 管交互
- HTML 由一对一对的标签组成，标签可以带属性
- 本节认识了 5 个常用标签：`h1`、`p`、`ul`/`li`、`a`、`img`

## 八、下节预告

下一节我们会学 **CSS**，让你这个朴素的页面变好看：换颜色、换字体、加边框、调布局。

---

# 📌 课后作业

## 题目：做一个"个人介绍页"

**要求：**

1. 新建一个文件叫 `about-me.html`
2. 用今天学到的 HTML 标签，做一个介绍**你自己**的页面
3. **至少**包含以下内容：
   - 一个一级标题 `<h1>`，写你的名字
   - 至少 2 段文字 `<p>`，介绍你自己（比如所在城市、职业、为什么想学编程）
   - 一个二级标题 `<h2>`，比如 "我的爱好"
   - 一个列表 `<ul>` + `<li>`，列出至少 3 个爱好
   - 一张图片 `<img>`（可以用网上随便找的图，把图片地址贴到 `src` 里）
   - 一个外链 `<a>`，指向你常用的一个网站

## 提交方式

把写好的 `about-me.html` 文件发给我，
**同时附上一张在 Chrome 里打开后的截图**。

## 加分项（可选）

- 试着把 `<ul>` 换成 `<ol>`，看看页面有什么变化，并用一句话告诉我区别
- 在页面里再加一个三级标题 `<h3>`

## 友情提示

- 写代码时**注意标签要成对闭合**，比如写了 `<p>` 一定要有 `</p>`
- 中文字符没问题，但**所有尖括号、引号必须是英文符号**（`<` `>` `"`），不要打成中文的`《》""`
- 写完保存后，用 Chrome 打开看看效果，有问题随时问我
